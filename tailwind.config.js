/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  // In Tailwind v4, theme customization is done via @theme directive in CSS
  // Dark mode and other configurations are handled automatically
} 